package com.nordea.dompap.workflow;

import org.junit.Test;

public class WorkflowSerializerConfigTest {

	@Test
	public void testConfigSerializer() {
		// TODO fix test
/*
		try (ModuleHolderContext ctx = new ModuleHolderContext(ModuleId.of("WorkflowSerializerConfigTest")))  {
			WorkFlowServiceImpl svc = new WorkFlowServiceImpl();
			assertEquals("a", svc.getConfiguredSerializer("com.acme.Test"));
			assertEquals("package", svc.getConfiguredSerializer("com.acme.OtherClass"));
			assertEquals("top-package", svc.getConfiguredSerializer("com.OtherClassInTop"));
			assertEquals("top-package", svc.getConfiguredSerializer("com.OtherClassInTop"));
		}

 */
	}
}
